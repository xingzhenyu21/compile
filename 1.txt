int main() {
    int n, m, num;
    n = getint();
    m = getint();
    int rem;
    while (n > 0) {
        rem = m % n;
        m = n;
        n = rem;
    }
    num = m;
    putint(num);
    return 0;
}